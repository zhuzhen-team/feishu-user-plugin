[
  {
    "name": "add_reaction",
    "description": "[Official API] Add an emoji reaction to a message.",
    "inputSchema": {
      "properties": {
        "emoji_type": {
          "description": "Emoji type string, e.g. \"THUMBSUP\", \"SMILE\", \"HEART\"",
          "type": "string"
        },
        "message_id": {
          "description": "Message ID (om_xxx)",
          "type": "string"
        }
      },
      "required": [
        "emoji_type",
        "message_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "batch_send",
    "description": "[User Identity / Official API] Send the same or different content to multiple targets in one call. Each target dispatches sequentially with a small delay (anti-rate-limit) and reports per-target success/error. Identity is the cookie user (user-identity sends) unless target.via=bot. Use for broadcast / fan-out scenarios.",
    "inputSchema": {
      "properties": {
        "delay_ms": {
          "description": "Delay between sends in milliseconds (default 200, increase for risky volumes).",
          "type": "number"
        },
        "targets": {
          "description": "Array of targets. Each entry: { type: \"user\"|\"group\"|\"chat\", id: <user_name | group_name | chat_id>, content: { kind: \"text\"|\"image\"|\"file\"|\"post\", ... } }. For kind=\"text\": { text }. For \"image\": { image_key }. For \"file\": { file_key, file_name }. For \"post\": { title, paragraphs }. Optional per-target: via=\"bot\" routes through send_message_as_bot (chat_id required).",
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "targets"
      ],
      "type": "object"
    }
  },
  {
    "name": "complete_task",
    "description": "[Official API + UAT, v1.3.7] Mark a task complete (or uncomplete it). Convenience wrapper around update_task with completed_at.",
    "inputSchema": {
      "properties": {
        "completed": {
          "description": "true → mark complete (uses Date.now()); false → uncomplete (sets completed_at to \"0\"). Default true.",
          "type": "boolean"
        },
        "task_guid": {
          "description": "Task GUID",
          "type": "string"
        }
      },
      "required": [
        "task_guid"
      ],
      "type": "object"
    }
  },
  {
    "name": "copy_wiki_node",
    "description": "[Official API] Deep-copy a Wiki node into a different location (and optionally a different space). Underlying resource is duplicated.",
    "inputSchema": {
      "properties": {
        "node_token": {
          "description": "Wiki node token to copy",
          "type": "string"
        },
        "space_id": {
          "description": "Source space ID",
          "type": "string"
        },
        "target_parent_token": {
          "description": "Destination parent wiki node token (optional)",
          "type": "string"
        },
        "target_space_id": {
          "description": "Destination space ID (optional; same-space copy if omitted)",
          "type": "string"
        },
        "title": {
          "description": "Title for the copy (optional; defaults to source title)",
          "type": "string"
        }
      },
      "required": [
        "node_token",
        "space_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_calendar_event",
    "description": "[Official API + UAT, v1.3.7] Create a new calendar event. Requires `calendar:calendar.event:write` scope (re-run `npx feishu-user-plugin oauth` after enabling). The current identity (UAT-first) must have writer or owner permission on the calendar.\n\nTime fields: A time object: {timestamp:\"<unix-seconds>\", timezone?:\"Asia/Shanghai\"} OR {date:\"YYYY-MM-DD\"} for all-day events.",
    "inputSchema": {
      "properties": {
        "attendee_ability": {
          "description": "What attendees may do (optional)",
          "enum": [
            "none",
            "can_see_others",
            "can_invite_others",
            "can_modify_event"
          ],
          "type": "string"
        },
        "calendar_id": {
          "description": "Calendar ID (use list_calendars; primary calendar has type=\"primary\").",
          "type": "string"
        },
        "description": {
          "description": "Description / notes (optional)",
          "type": "string"
        },
        "end_time": {
          "description": "A time object: {timestamp:\"<unix-seconds>\", timezone?:\"Asia/Shanghai\"} OR {date:\"YYYY-MM-DD\"} for all-day events.",
          "type": "object"
        },
        "free_busy_status": {
          "description": "Whether this event blocks the calendar (optional)",
          "enum": [
            "busy",
            "free"
          ],
          "type": "string"
        },
        "location": {
          "description": "Optional. {name, address?, latitude?, longitude?}.",
          "type": "object"
        },
        "need_notification": {
          "description": "Whether to notify attendees on create (default true)",
          "type": "boolean"
        },
        "recurrence": {
          "description": "iCal RRULE recurrence string (optional)",
          "type": "string"
        },
        "reminders": {
          "description": "Reminders before event start (optional). E.g. [{minutes:15}].",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "start_time": {
          "description": "A time object: {timestamp:\"<unix-seconds>\", timezone?:\"Asia/Shanghai\"} OR {date:\"YYYY-MM-DD\"} for all-day events.",
          "type": "object"
        },
        "summary": {
          "description": "Event title",
          "type": "string"
        },
        "visibility": {
          "description": "Event visibility (optional)",
          "enum": [
            "default",
            "public",
            "private"
          ],
          "type": "string"
        }
      },
      "required": [
        "calendar_id",
        "end_time",
        "start_time",
        "summary"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_doc",
    "description": "[Official API] Create a new Feishu document. Can place directly under a Wiki space by passing wiki_space_id (optionally wiki_parent_node_token for nested placement) — the plugin creates the doc in drive then attaches it as a Wiki node.",
    "inputSchema": {
      "properties": {
        "folder_id": {
          "description": "Parent folder token (optional; ignored when wiki_space_id is set)",
          "type": "string"
        },
        "title": {
          "description": "Document title",
          "type": "string"
        },
        "wiki_parent_node_token": {
          "description": "Parent wiki node token within the space (optional; defaults to space root)",
          "type": "string"
        },
        "wiki_space_id": {
          "description": "Wiki space ID to place the doc under (optional)",
          "type": "string"
        }
      },
      "required": [
        "title"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_folder",
    "description": "[Official API] Create a new folder in Drive.",
    "inputSchema": {
      "properties": {
        "name": {
          "description": "Folder name",
          "type": "string"
        },
        "parent_token": {
          "description": "Parent folder token (optional)",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_group",
    "description": "[Official API] Create a new group chat (as bot). Can add initial members.",
    "inputSchema": {
      "properties": {
        "description": {
          "description": "Group description (optional)",
          "type": "string"
        },
        "name": {
          "description": "Group name",
          "type": "string"
        },
        "user_ids": {
          "description": "Initial member open_ids (optional)",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_okr_progress_record",
    "description": "[Official API + UAT, v1.3.7] Add a progress note to an OKR objective or key result. Feishu requires `source_title`, `source_url`, and a block-structured `content`; this tool exposes a simple `content_text` and auto-wraps it into the single-paragraph block format. Pass richer `content` directly if you need lists / mentions / docs links / images.",
    "inputSchema": {
      "properties": {
        "content": {
          "description": "Optional: full Feishu block structure ({blocks:[...]}). If provided, overrides content_text.",
          "type": "object"
        },
        "content_text": {
          "description": "Plain-text progress note. Auto-wrapped into the Feishu block format. Use `content` instead for rich text.",
          "type": "string"
        },
        "progress_percent": {
          "description": "Optional progress percent (0-100) to bump alongside the note.",
          "type": "number"
        },
        "progress_status": {
          "description": "Optional status code (Feishu enum: 1=on track, 2=at risk, 3=blocked, etc).",
          "type": "number"
        },
        "source_title": {
          "description": "Source label (default \"Progress update\"). Shown next to the note in the OKR UI.",
          "type": "string"
        },
        "source_url": {
          "description": "Source URL (default https://feishu.cn/). Feishu requires a URL even for plain notes.",
          "type": "string"
        },
        "source_url_mobile": {
          "description": "Optional mobile-specific source URL.",
          "type": "string"
        },
        "source_url_pc": {
          "description": "Optional PC-specific source URL.",
          "type": "string"
        },
        "target_id": {
          "description": "ID of the OKR objective or key result. Get from get_okrs response (`objective_list[].id` or `objective_list[].kr_list[].id`).",
          "type": "string"
        },
        "target_type": {
          "description": "1 = objective, 2 = key result. Pick based on which level target_id refers to.",
          "type": "number"
        },
        "user_id_type": {
          "description": "Type of user IDs in mentioned_user_list etc. (default open_id)",
          "enum": [
            "user_id",
            "union_id",
            "open_id"
          ],
          "type": "string"
        }
      },
      "required": [
        "target_id",
        "target_type"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_p2p_chat",
    "description": "[User Identity] Create or get a P2P (direct message) chat. Returns numeric chat_id.",
    "inputSchema": {
      "properties": {
        "user_id": {
          "description": "Target user ID from search_contacts",
          "type": "string"
        }
      },
      "required": [
        "user_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_task",
    "description": "[Official API + UAT, v1.3.7] Create a new task. summary is required; due / members / etc. are optional.",
    "inputSchema": {
      "properties": {
        "description": {
          "description": "Task description (optional)",
          "type": "string"
        },
        "due": {
          "description": "Due time (optional). {timestamp:\"<unix-millis>\", is_all_day?:true|false}",
          "type": "object"
        },
        "extra": {
          "description": "Free-form extra metadata (optional)",
          "type": "string"
        },
        "members": {
          "description": "Initial members (optional). Each: {id:\"<open_id>\", role:\"assignee\"|\"follower\", type?:\"user\", name?:\"...\"}",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "repeat_rule": {
          "description": "Recurrence (optional, RFC5545 RRULE)",
          "type": "string"
        },
        "summary": {
          "description": "Task title",
          "type": "string"
        }
      },
      "required": [
        "summary"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_wiki_node",
    "description": "[Official API] Create a new Wiki node inside a space. obj_type picks the underlying resource (doc/sheet/bitable/mindnote/file/docx/slides). UAT-first so the resource is owned by the user.",
    "inputSchema": {
      "properties": {
        "node_type": {
          "default": "origin",
          "description": "origin = real resource, shortcut = pointer to existing node (default: origin)",
          "enum": [
            "origin",
            "shortcut"
          ],
          "type": "string"
        },
        "obj_type": {
          "description": "Underlying resource type",
          "enum": [
            "doc",
            "sheet",
            "bitable",
            "mindnote",
            "file",
            "docx",
            "slides"
          ],
          "type": "string"
        },
        "origin_node_token": {
          "description": "Required when node_type=shortcut — the wiki node this shortcut points at",
          "type": "string"
        },
        "parent_node_token": {
          "description": "Parent wiki node under which to create (optional; root if omitted)",
          "type": "string"
        },
        "space_id": {
          "description": "Wiki space ID (from list_wiki_spaces)",
          "type": "string"
        },
        "title": {
          "description": "Node title (optional; Feishu generates a default if absent)",
          "type": "string"
        }
      },
      "required": [
        "obj_type",
        "space_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "delete_calendar_event",
    "description": "[Official API + UAT, v1.3.7] Delete a calendar event. Requires `calendar:calendar.event:write` scope.",
    "inputSchema": {
      "properties": {
        "calendar_id": {
          "description": "Calendar ID",
          "type": "string"
        },
        "event_id": {
          "description": "Event ID",
          "type": "string"
        },
        "meeting_chat_id": {
          "description": "Optional. If the event has a linked meeting chat, pass its chat_id to also dissolve it.",
          "type": "string"
        },
        "need_notification": {
          "description": "Whether to notify attendees of the deletion (default true)",
          "type": "boolean"
        }
      },
      "required": [
        "calendar_id",
        "event_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "delete_message",
    "description": "[Official API] Recall/delete a message (bot can only delete its own messages).",
    "inputSchema": {
      "properties": {
        "message_id": {
          "description": "Message ID (om_xxx)",
          "type": "string"
        }
      },
      "required": [
        "message_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "delete_okr_progress_record",
    "description": "[Official API + UAT, v1.3.7] Delete an OKR progress record by its progress_id (from list_okr_progress_records).",
    "inputSchema": {
      "properties": {
        "progress_id": {
          "description": "Progress record ID",
          "type": "string"
        }
      },
      "required": [
        "progress_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "delete_reaction",
    "description": "[Official API] Remove an emoji reaction from a message.",
    "inputSchema": {
      "properties": {
        "message_id": {
          "description": "Message ID",
          "type": "string"
        },
        "reaction_id": {
          "description": "Reaction ID (from add_reaction response)",
          "type": "string"
        }
      },
      "required": [
        "message_id",
        "reaction_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "delete_task",
    "description": "[Official API + UAT, v1.3.7] Permanently delete a task.",
    "inputSchema": {
      "properties": {
        "task_guid": {
          "description": "Task GUID",
          "type": "string"
        }
      },
      "required": [
        "task_guid"
      ],
      "type": "object"
    }
  },
  {
    "name": "delete_wiki_node",
    "description": "[Official API, v1.3.7] Delete a Wiki node. Calls `DELETE /open-apis/wiki/v2/spaces/{space_id}/nodes/{node_token}`. The Feishu SDK does not type this endpoint, so the call goes through raw REST (UAT-first; bot fallback uses `client.request`). **The underlying drive resource (docx / sheet / bitable / file) is NOT deleted** — Feishu treats wiki nodes as pointers. To delete the actual resource as well, follow up with `manage_drive_file(action=delete, type=<obj_type>, file_token=<obj_token>)` (use `get_wiki_node` first to get obj_type / obj_token).",
    "inputSchema": {
      "properties": {
        "node_token": {
          "description": "Wiki node token to delete",
          "type": "string"
        },
        "space_id": {
          "description": "Wiki space ID",
          "type": "string"
        }
      },
      "required": [
        "node_token",
        "space_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "download_doc_image",
    "description": "[User Identity / Official API] Download an image embedded in a docx document so the model can see it. Pass the `image_token` from `get_doc_blocks` (block.image.token), and optionally the doc/wiki/URL token to scope the lookup. UAT-first.\n\n**Size cap:** payloads > 2 MiB MUST pass `save_path`.",
    "inputSchema": {
      "properties": {
        "doc_token": {
          "description": "Document ID, wiki node token, or Feishu URL (optional but recommended for permission scoping).",
          "type": "string"
        },
        "image_token": {
          "description": "Image token (from get_doc_blocks image block)",
          "type": "string"
        },
        "save_path": {
          "description": "Absolute local path. Required when image bytes > 2 MiB.",
          "type": "string"
        }
      },
      "required": [
        "image_token"
      ],
      "type": "object"
    }
  },
  {
    "name": "download_message_resource",
    "description": "[User Identity / Official API] Download an image or file attached to a message so the model can see / store it. v1.3.7 (C2.4) consolidates the v1.3.6 download_image (mode 1) + download_file. UAT-first, falls back to app.\n\nFor images, the response includes an inline `image` content block so the model sees pixels. For files, the response includes the bytes as base64 (truncated for display) plus an optional save_path write.\n\n**Size cap:** payloads > 2 MiB MUST pass `save_path`. The Anthropic API rejects responses > 5 MB; we cap at 2 MiB so multipart wrapping has headroom.\n\n**merge_forward children:** Feishu keys media by the parent merge_forward id, not the child id. Use the child's `parentMessageId` field (returned by read_messages with expand_merge_forward) — not the child id.",
    "inputSchema": {
      "properties": {
        "key": {
          "description": "image_key (img_xxx) for kind=image, file_key for kind=file. From read_messages content.",
          "type": "string"
        },
        "kind": {
          "description": "image or file",
          "enum": [
            "image",
            "file"
          ],
          "type": "string"
        },
        "message_id": {
          "description": "Message ID (om_xxx). For merge_forward children, use the child's `parentMessageId`.",
          "type": "string"
        },
        "save_path": {
          "description": "Absolute local path. Required when downloaded bytes > 2 MiB (else the response would exceed the Anthropic API 5 MB inline limit).",
          "type": "string"
        }
      },
      "required": [
        "key",
        "kind",
        "message_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "forward_message",
    "description": "[Official API] Forward a message to another chat or user. `receive_id` may be a group chat_id (oc_xxx), an open_id (ou_xxx), a union_id, a user_id, or an email — set `receive_id_type` to match (default: chat_id).",
    "inputSchema": {
      "properties": {
        "message_id": {
          "description": "Message ID to forward (om_xxx)",
          "type": "string"
        },
        "receive_id": {
          "description": "Target chat_id (oc_xxx), open_id (ou_xxx), union_id, user_id, or email — set receive_id_type to match.",
          "type": "string"
        },
        "receive_id_type": {
          "default": "chat_id",
          "description": "Format of receive_id (default: chat_id). Set to \"open_id\" when forwarding to a user via their open_id.",
          "enum": [
            "chat_id",
            "open_id",
            "union_id",
            "user_id",
            "email"
          ],
          "type": "string"
        }
      },
      "required": [
        "message_id",
        "receive_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_calendar_event",
    "description": "[Official API + UAT] Get full details of a single calendar event (summary, description, start/end, attendees, location, attachments, meeting link).",
    "inputSchema": {
      "properties": {
        "calendar_id": {
          "description": "Calendar ID",
          "type": "string"
        },
        "event_id": {
          "description": "Event ID from list_calendar_events",
          "type": "string"
        }
      },
      "required": [
        "calendar_id",
        "event_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_chat_info",
    "description": "[Official API + User Identity fallback] Get chat details: name, description, member count, owner. Supports both oc_xxx and numeric chat_id.",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Chat ID (oc_xxx or numeric)",
          "type": "string"
        }
      },
      "required": [
        "chat_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_doc_blocks",
    "description": "[Official API] Get structured block tree of a document. Returns block types, content, and hierarchy for precise document analysis.",
    "inputSchema": {
      "properties": {
        "document_id": {
          "description": "Document ID (from search_docs or create_doc)",
          "type": "string"
        }
      },
      "required": [
        "document_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_freebusy",
    "description": "[Official API + UAT, v1.3.7] Query freebusy windows for one or more users in a time range. Use to find a meeting slot. Requires `calendar:calendar:readonly` (already in default scope set).",
    "inputSchema": {
      "properties": {
        "include_external_calendar": {
          "description": "Include the user's synced external calendars (optional)",
          "type": "boolean"
        },
        "only_busy": {
          "description": "Only return busy windows (optional)",
          "type": "boolean"
        },
        "room_ids": {
          "description": "Optional meeting-room IDs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time_max": {
          "description": "RFC3339 end",
          "type": "string"
        },
        "time_min": {
          "description": "RFC3339 start, e.g. 2026-05-04T09:00:00+08:00",
          "type": "string"
        },
        "user_ids": {
          "description": "Open IDs to query (use get_login_status / search_contacts to look up).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "time_max",
        "time_min",
        "user_ids"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_login_status",
    "description": "Check cookie session validity and app credentials status. Also refreshes session.",
    "inputSchema": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "get_new_events",
    "description": "[Plugin v1.3.8] Drain real-time events received since the last call. Currently surfaces \"im.message.receive_v1\" events (replies, group messages). Returns empty when WS isn't connected or no events have arrived. Use filter to scope by event_type or chat_id; max_events caps response size.",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Optional: only events from this chat (oc_xxx for groups, message events expose chat_id).",
          "type": "string"
        },
        "event_type": {
          "description": "Optional: only events of this type (e.g. \"im.message.receive_v1\").",
          "type": "string"
        },
        "event_types": {
          "description": "Optional: any-of list of event types.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_events": {
          "description": "Cap on returned events (default 50). Drained events beyond the cap are returned in subsequent calls.",
          "type": "integer"
        },
        "peek": {
          "description": "When true, leave events in the buffer (default false = drain).",
          "type": "boolean"
        },
        "since_seconds": {
          "description": "Optional: only events received in the last N seconds.",
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "get_okrs",
    "description": "[Official API + UAT] Batch-fetch full OKR details (objectives, key results, progress, alignments) by OKR IDs.",
    "inputSchema": {
      "properties": {
        "lang": {
          "description": "Response language (optional)",
          "type": "string"
        },
        "okr_ids": {
          "description": "OKR IDs (max 10 per call). From list_user_okrs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "user_id_type": {
          "description": "Type of user_ids in response (default: open_id)",
          "enum": [
            "user_id",
            "union_id",
            "open_id",
            "people_admin_id"
          ],
          "type": "string"
        }
      },
      "required": [
        "okr_ids"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_task",
    "description": "[Official API + UAT, v1.3.7] Get full details of a single task by GUID.",
    "inputSchema": {
      "properties": {
        "task_guid": {
          "description": "Task GUID (from list_tasks / create_task / Feishu URL)",
          "type": "string"
        }
      },
      "required": [
        "task_guid"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_user_info",
    "description": "[User Identity] Look up a user's display name by user ID.",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Chat context (optional)",
          "type": "string"
        },
        "user_id": {
          "description": "User ID",
          "type": "string"
        }
      },
      "required": [
        "user_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_wiki_node",
    "description": "[Official API] Resolve a Wiki node token to its underlying object (docx / bitable / sheet / mindnote / file). Returns obj_type + obj_token + space_id so you can read/write the real resource via the usual docx / bitable tools. Accepts bare wiki node token (wikcnXXX), an underlying obj_token (docxXXX / bascnXXX from search_wiki), or a full Feishu /wiki/ URL — the handler tries the wiki endpoint first and falls back to a synthesized node-shape for non-wiki tokens.",
    "inputSchema": {
      "properties": {
        "node_token": {
          "description": "Wiki node token (wikcnXXX / wikmXXX / wiknXXX), underlying obj_token (docxXXX / bascnXXX), or full Feishu /wiki/<token> URL",
          "type": "string"
        }
      },
      "required": [
        "node_token"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_calendar_events",
    "description": "[Official API + UAT] List events in a calendar within an optional time range. Typical usage: first list_calendars to find calendar_id (primary calendar has type=\"primary\"), then list events in e.g. [now, now+7d] (Unix seconds).",
    "inputSchema": {
      "properties": {
        "calendar_id": {
          "description": "Calendar ID from list_calendars",
          "type": "string"
        },
        "end_time": {
          "description": "Range end (Unix seconds, optional)",
          "type": "string"
        },
        "page_size": {
          "description": "Items per page (default 50)",
          "type": "number"
        },
        "page_token": {
          "description": "Pagination token",
          "type": "string"
        },
        "start_time": {
          "description": "Range start (Unix seconds, optional)",
          "type": "string"
        },
        "sync_token": {
          "description": "Incremental sync token (optional)",
          "type": "string"
        }
      },
      "required": [
        "calendar_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_calendars",
    "description": "[Official API + UAT] List the current user's calendars (primary + shared + subscribed). Requires UAT — app identity only sees calendars it was explicitly invited to. Requires `calendar:calendar:readonly` scope on the OAuth.",
    "inputSchema": {
      "properties": {
        "page_size": {
          "description": "Items per page (min 50, default 50). Feishu's calendar endpoint rejects page_size < 50.",
          "type": "number"
        },
        "page_token": {
          "description": "Pagination token",
          "type": "string"
        },
        "sync_token": {
          "description": "Incremental sync token (optional)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "list_chats",
    "description": "[Official API] List all chats the bot has joined. Returns chat_id, name, type.",
    "inputSchema": {
      "properties": {
        "page_size": {
          "description": "Items per page (default 20, max 100)",
          "type": "number"
        },
        "page_token": {
          "description": "Pagination token",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "list_files",
    "description": "[Official API] List files in a Drive folder.",
    "inputSchema": {
      "properties": {
        "folder_token": {
          "description": "Folder token (empty for root)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "list_members",
    "description": "[Official API] List all members in a group chat.",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Chat ID (oc_xxx)",
          "type": "string"
        },
        "page_size": {
          "description": "Items per page (default 50)",
          "type": "number"
        },
        "page_token": {
          "description": "Pagination token",
          "type": "string"
        }
      },
      "required": [
        "chat_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_okr_periods",
    "description": "[Official API + UAT] List OKR periods (quarters / years) defined in the tenant. Use period_ids from this to filter list_user_okrs.",
    "inputSchema": {
      "properties": {
        "page_size": {
          "description": "Items per page (default 10)",
          "type": "number"
        },
        "page_token": {
          "description": "Pagination token",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "list_okr_progress_records",
    "description": "[Official API + UAT, v1.3.7] List progress records for an OKR. Feishu has no native list endpoint — this tool calls get_okrs internally and walks the objective_list / kr_list to extract progress_record IDs (with their target_id and target_type). To read a record's full content, you currently need progressRecord.get (not yet wrapped).",
    "inputSchema": {
      "properties": {
        "okr_id": {
          "description": "OKR ID (from list_user_okrs).",
          "type": "string"
        },
        "user_id_type": {
          "description": "Pass-through to get_okrs (default open_id)",
          "enum": [
            "user_id",
            "union_id",
            "open_id"
          ],
          "type": "string"
        }
      },
      "required": [
        "okr_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_profiles",
    "description": "[Plugin] List all available identity profiles (sets of LARK_COOKIE/APP_ID/APP_SECRET/UAT). The \"default\" profile uses the top-level env vars; additional profiles come from LARK_PROFILES_JSON. Marks the currently active profile.",
    "inputSchema": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "list_tasks",
    "description": "[Official API + UAT, v1.3.7] List the current user's tasks. Filter by completion or type.",
    "inputSchema": {
      "properties": {
        "completed": {
          "description": "true → only completed; false → only pending; omit → all",
          "type": "boolean"
        },
        "page_size": {
          "description": "Items per page (default Feishu default)",
          "type": "number"
        },
        "page_token": {
          "description": "Pagination token",
          "type": "string"
        },
        "type": {
          "description": "Filter by task type (optional). E.g. \"all\" / \"personal\".",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "list_user_chats",
    "description": "[User UAT] List group chats the user is in. Note: only returns groups, not P2P. For P2P chats, use search_contacts → create_p2p_chat → read_p2p_messages. Requires OAuth setup.",
    "inputSchema": {
      "properties": {
        "page_size": {
          "description": "Items per page (default 20)",
          "type": "number"
        },
        "page_token": {
          "description": "Pagination token",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "list_user_okrs",
    "description": "[Official API + UAT] List a user's OKRs. Requires the user's open_id (get yours via get_login_status or search_contacts). Filter by period_ids to narrow to a specific quarter.",
    "inputSchema": {
      "properties": {
        "lang": {
          "description": "Response language (optional, e.g. \"zh_cn\", \"en_us\")",
          "type": "string"
        },
        "limit": {
          "description": "Items per page (default 10, max 10)",
          "type": "number"
        },
        "offset": {
          "description": "Pagination offset (default 0)",
          "type": "number"
        },
        "period_ids": {
          "description": "Filter by OKR period IDs (optional). Get period IDs via list_okr_periods.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "user_id": {
          "description": "Target user's open_id (or the matching user_id_type)",
          "type": "string"
        },
        "user_id_type": {
          "description": "Type of user_id (default: open_id)",
          "enum": [
            "user_id",
            "union_id",
            "open_id",
            "people_admin_id"
          ],
          "type": "string"
        }
      },
      "required": [
        "user_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_wiki_nodes",
    "description": "[Official API] List nodes in a Wiki space.",
    "inputSchema": {
      "properties": {
        "parent_node_token": {
          "description": "Parent node token (optional)",
          "type": "string"
        },
        "space_id": {
          "description": "Wiki space ID",
          "type": "string"
        }
      },
      "required": [
        "space_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_wiki_spaces",
    "description": "[Official API] List all accessible Wiki spaces.",
    "inputSchema": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "manage_bitable_app",
    "description": "[Official API] Manage a Bitable app. action=create (new app, optional wiki_space_id to attach), copy (duplicate an existing app), get_meta (read app metadata).",
    "inputSchema": {
      "properties": {
        "action": {
          "description": "Operation to perform",
          "enum": [
            "create",
            "copy",
            "get_meta"
          ],
          "type": "string"
        },
        "app_token": {
          "description": "Required for copy/get_meta. Native token, wiki node, or Feishu URL.",
          "type": "string"
        },
        "folder_id": {
          "description": "Destination folder token (optional for create/copy; ignored when wiki_space_id is set).",
          "type": "string"
        },
        "name": {
          "description": "New app name. Required for create/copy.",
          "type": "string"
        },
        "wiki_parent_node_token": {
          "description": "Parent wiki node within the space (optional for create).",
          "type": "string"
        },
        "wiki_space_id": {
          "description": "Wiki space ID — create the app directly under this space (create only).",
          "type": "string"
        }
      },
      "required": [
        "action"
      ],
      "type": "object"
    }
  },
  {
    "name": "manage_bitable_field",
    "description": "[Official API] Manage fields (columns) inside a Bitable table. action=list, create, update (Feishu requires `type` even when only renaming), delete.",
    "inputSchema": {
      "properties": {
        "action": {
          "description": "Operation to perform",
          "enum": [
            "list",
            "create",
            "update",
            "delete"
          ],
          "type": "string"
        },
        "app_token": {
          "description": "Bitable app token. Accepts native token, wiki node, or Feishu URL.",
          "type": "string"
        },
        "field_id": {
          "description": "Field ID — required for update/delete.",
          "type": "string"
        },
        "field_name": {
          "description": "Field display name — required for create, optional for update.",
          "type": "string"
        },
        "property": {
          "description": "Field-type-specific properties (optional). E.g. SingleSelect: {options:[{name:\"A\"},{name:\"B\"}]}.",
          "type": "object"
        },
        "table_id": {
          "description": "Table ID",
          "type": "string"
        },
        "type": {
          "description": "Field type (1=Text, 2=Number, 3=SingleSelect, 4=MultiSelect, 5=DateTime, 7=Checkbox, 11=User, 13=Phone, 15=URL, 17=Attachment, 18=Link, 20=Formula, 21=DuplexLink, 22=Location, 23=GroupChat, 1001=CreateTime, 1002=ModifiedTime, 1003=Creator, 1004=Modifier). Required for create AND update — Feishu API rejects update without it.",
          "type": "number"
        }
      },
      "required": [
        "action",
        "app_token",
        "table_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "manage_bitable_record",
    "description": "[Official API] Manage records (rows) inside a Bitable table. action=search, get, create, update, delete. create/update/delete accept arrays — single record or up to 500.",
    "inputSchema": {
      "properties": {
        "action": {
          "description": "Operation to perform",
          "enum": [
            "search",
            "get",
            "create",
            "update",
            "delete"
          ],
          "type": "string"
        },
        "app_token": {
          "description": "Bitable app token. Accepts native token, wiki node, or Feishu URL.",
          "type": "string"
        },
        "filter": {
          "description": "Filter conditions (search only, optional)",
          "type": "object"
        },
        "page_size": {
          "description": "Results per page (search only, default 20)",
          "type": "number"
        },
        "record_id": {
          "description": "Record ID — required for action=get.",
          "type": "string"
        },
        "record_ids": {
          "description": "Record IDs to delete. Single ID or up to 500.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "records": {
          "description": "Records to write. For create: [{fields:{field_name:value}}]. For update: [{record_id, fields:{...}}]. Single record or up to 500.",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "sort": {
          "description": "Sort conditions (search only, optional)",
          "type": "array"
        },
        "table_id": {
          "description": "Table ID",
          "type": "string"
        }
      },
      "required": [
        "action",
        "app_token",
        "table_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "manage_bitable_table",
    "description": "[Official API] Manage a table inside a Bitable app. action=list, create (with optional initial fields), update (rename), delete.",
    "inputSchema": {
      "properties": {
        "action": {
          "description": "Operation to perform",
          "enum": [
            "list",
            "create",
            "update",
            "delete"
          ],
          "type": "string"
        },
        "app_token": {
          "description": "Bitable app token (required for all actions). Accepts native token, wiki node, or Feishu URL.",
          "type": "string"
        },
        "fields": {
          "description": "Initial field definitions (create only, optional). Each item: {field_name, type, property?} where type is 1=Text, 2=Number, 3=SingleSelect, 4=MultiSelect, 5=DateTime, 7=Checkbox, 11=User, 13=Phone, 15=URL, 17=Attachment, 18=Link, 20=Formula, 21=DuplexLink, 22=Location, 23=GroupChat, 1001=CreateTime, 1002=ModifiedTime, 1003=Creator, 1004=Modifier.",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "name": {
          "description": "Table name — required for create, optional for update (rename).",
          "type": "string"
        },
        "table_id": {
          "description": "Table ID — required for update/delete.",
          "type": "string"
        }
      },
      "required": [
        "action",
        "app_token"
      ],
      "type": "object"
    }
  },
  {
    "name": "manage_bitable_view",
    "description": "[Official API] Manage views inside a Bitable table. action=list, create, delete. (Feishu open API does not expose view update — recreate with a new name to change.)",
    "inputSchema": {
      "properties": {
        "action": {
          "description": "Operation to perform",
          "enum": [
            "list",
            "create",
            "delete"
          ],
          "type": "string"
        },
        "app_token": {
          "description": "Bitable app token. Accepts native token, wiki node, or Feishu URL.",
          "type": "string"
        },
        "table_id": {
          "description": "Table ID",
          "type": "string"
        },
        "view_id": {
          "description": "View ID — required for delete.",
          "type": "string"
        },
        "view_name": {
          "description": "View name — required for create.",
          "type": "string"
        },
        "view_type": {
          "default": "grid",
          "description": "View type for create: grid (default), kanban, gallery, form, gantt, calendar.",
          "type": "string"
        }
      },
      "required": [
        "action",
        "app_token",
        "table_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "manage_doc_block",
    "description": "[Official API] Manage content blocks in a document. Single tool replaces v1.3.6 create_doc_block / update_doc_block / delete_doc_blocks.\n  action=create — five modes:\n    (A) Generic — pass `children` array (e.g. [{block_type:2, text:{...}}]).\n    (B) Image from local file — pass `image_path`; plugin uploads and patches.\n    (C) Image from token — pass `image_token` (already uploaded).\n    (D) File attachment from local file — pass `file_path`; plugin handles VIEW-wrap + replace_file.\n    (E) File from token — pass `file_token`.\n  action=update — generic (pass `update_body`), image-replace (pass `image_token`), or file-replace (pass `file_token`).\n  action=delete — pass `parent_block_id` + `start_index` + `end_index` (range delete).\n`document_id` accepts native ID, wiki node token, or Feishu URL.",
    "inputSchema": {
      "properties": {
        "action": {
          "description": "Operation to perform",
          "enum": [
            "create",
            "update",
            "delete"
          ],
          "type": "string"
        },
        "block_id": {
          "description": "Block ID — required for action=update.",
          "type": "string"
        },
        "children": {
          "description": "Generic blocks for create mode A. E.g. [{block_type:2, text:{elements:[{text_run:{content:\"Hello\"}}]}}]",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "document_id": {
          "description": "Document ID, wiki node token, or Feishu URL (required for all actions)",
          "type": "string"
        },
        "end_index": {
          "description": "Range end (exclusive) — required for delete.",
          "type": "number"
        },
        "file_path": {
          "description": "Local file path — create mode D (mutually exclusive with other create modes).",
          "type": "string"
        },
        "file_token": {
          "description": "Pre-uploaded docx file token — create mode E, or update file-replace.",
          "type": "string"
        },
        "image_path": {
          "description": "Local image path — create mode B (mutually exclusive with other create modes).",
          "type": "string"
        },
        "image_token": {
          "description": "Pre-uploaded docx image token — create mode C, or update image-replace.",
          "type": "string"
        },
        "index": {
          "description": "Insert position for create (optional, appends to end if omitted).",
          "type": "number"
        },
        "parent_block_id": {
          "description": "Parent block ID — required for create/delete (use document_id for the doc root).",
          "type": "string"
        },
        "start_index": {
          "description": "Range start (inclusive) — required for delete.",
          "type": "number"
        },
        "update_body": {
          "description": "Generic update payload for action=update. E.g. {update_text_elements:{elements:[{text_run:{content:\"new text\"}}]}}.",
          "type": "object"
        }
      },
      "required": [
        "action",
        "document_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "manage_drive_file",
    "description": "[Official API] Manage a Drive file/doc/folder. action=copy (duplicate to a new name + folder), move (relocate, returns task_id), delete (remove, returns task_id). `type` is always required (Feishu rejects with 1061002 / 1062501 otherwise).",
    "inputSchema": {
      "properties": {
        "action": {
          "description": "Operation to perform",
          "enum": [
            "copy",
            "move",
            "delete"
          ],
          "type": "string"
        },
        "file_token": {
          "description": "File/folder token to operate on (required for all actions).",
          "type": "string"
        },
        "folder_token": {
          "description": "Destination folder token — required for action=move; optional for action=copy (defaults to root).",
          "type": "string"
        },
        "name": {
          "description": "New name — required for action=copy.",
          "type": "string"
        },
        "type": {
          "description": "Resource type — Feishu requires this to know which API table to look up.",
          "enum": [
            "file",
            "folder",
            "doc",
            "sheet",
            "bitable",
            "docx",
            "mindnote",
            "slides"
          ],
          "type": "string"
        }
      },
      "required": [
        "action",
        "file_token",
        "type"
      ],
      "type": "object"
    }
  },
  {
    "name": "manage_members",
    "description": "[Official API] Add or remove members from a group chat. The Feishu API rejects with code 9499 when the IDs in `member_ids` do not match `member_id_type` — pass `member_id_type` explicitly when using union_id or user_id (default: open_id).",
    "inputSchema": {
      "properties": {
        "action": {
          "description": "Action to perform",
          "enum": [
            "add",
            "remove"
          ],
          "type": "string"
        },
        "chat_id": {
          "description": "Group chat ID (oc_xxx)",
          "type": "string"
        },
        "member_id_type": {
          "default": "open_id",
          "description": "Format of member_ids (default: open_id).",
          "enum": [
            "open_id",
            "union_id",
            "user_id"
          ],
          "type": "string"
        },
        "member_ids": {
          "description": "Array of member identifiers — IDs must match member_id_type.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "action",
        "chat_id",
        "member_ids"
      ],
      "type": "object"
    }
  },
  {
    "name": "manage_profile_hints",
    "description": "[Plugin v1.3.8] Inspect / set / clear profileHints — the resourceKey → profileName cache the auto-switch middleware uses to remember which profile owns each Feishu resource. Useful when a hint goes stale (e.g., a profile lost access to a doc).",
    "inputSchema": {
      "properties": {
        "action": {
          "description": "list = show all hints; set = upsert one; clear = remove one or all.",
          "enum": [
            "list",
            "set",
            "clear"
          ],
          "type": "string"
        },
        "profile": {
          "description": "For set: the profile name to associate with the resource_key.",
          "type": "string"
        },
        "resource_key": {
          "description": "For set/clear: the resourceKey, e.g. \"doc:doccnXXX\" or \"chat:oc_zzz\". Omit on clear to wipe all hints.",
          "type": "string"
        }
      },
      "required": [
        "action"
      ],
      "type": "object"
    }
  },
  {
    "name": "manage_task_members",
    "description": "[Official API + UAT, v1.3.7] Add or remove members on a task. Members are objects {id:\"<open_id>\", role:\"assignee\"|\"follower\", type?:\"user\", name?:\"\"}.",
    "inputSchema": {
      "properties": {
        "action": {
          "description": "add or remove",
          "enum": [
            "add",
            "remove"
          ],
          "type": "string"
        },
        "members": {
          "description": "Members to add/remove. Each: {id, role, type?, name?}.",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "task_guid": {
          "description": "Task GUID",
          "type": "string"
        }
      },
      "required": [
        "action",
        "members",
        "task_guid"
      ],
      "type": "object"
    }
  },
  {
    "name": "move_wiki_node",
    "description": "[Official API] Move a Wiki node to a different parent (within the same space) or to a different space. Pass at least one of target_parent_token / target_space_id.",
    "inputSchema": {
      "properties": {
        "node_token": {
          "description": "Wiki node token to move",
          "type": "string"
        },
        "space_id": {
          "description": "Source space ID",
          "type": "string"
        },
        "target_parent_token": {
          "description": "New parent wiki node token (optional)",
          "type": "string"
        },
        "target_space_id": {
          "description": "New target space ID (optional; same-space move if omitted)",
          "type": "string"
        }
      },
      "required": [
        "node_token",
        "space_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "pin_message",
    "description": "[Official API] Pin or unpin a message in a chat.",
    "inputSchema": {
      "properties": {
        "message_id": {
          "description": "Message ID",
          "type": "string"
        },
        "pinned": {
          "default": true,
          "description": "true to pin, false to unpin",
          "type": "boolean"
        }
      },
      "required": [
        "message_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "read_doc",
    "description": "[Official API] Read the raw text content of a Feishu document.",
    "inputSchema": {
      "properties": {
        "document_id": {
          "description": "Document ID or token",
          "type": "string"
        }
      },
      "required": [
        "document_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "read_messages",
    "description": "[Official API + UAT fallback] Read message history from any group. Accepts oc_xxx ID, numeric ID, or chat name (auto-searched). Auto-falls back to UAT for external groups the bot cannot access. Returns newest messages first by default, with sender names resolved. Auto-expands merge_forward messages into their child messages (with original sender / time / content preserved) by default — disable with expand_merge_forward=false. Text messages have URLs extracted into `urls`; Feishu doc links are additionally surfaced as `feishuDocs` so agents can feed them straight into read_doc / get_doc_blocks.",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Chat ID (oc_xxx), numeric ID, or chat name (auto-searched via bot groups, im.chat.search, and user contacts)",
          "type": "string"
        },
        "end_time": {
          "description": "End timestamp in seconds (optional)",
          "type": "string"
        },
        "expand_merge_forward": {
          "description": "Auto-expand merge_forward placeholders into their child messages (default true). Children carry parentMessageId; use that id (not the child id) with download_message_resource (kind=image or file).",
          "type": "boolean"
        },
        "page_size": {
          "description": "Messages to fetch (default 20, max 50)",
          "type": "number"
        },
        "sort_type": {
          "description": "Sort order (default: ByCreateTimeDesc = newest first)",
          "enum": [
            "ByCreateTimeDesc",
            "ByCreateTimeAsc"
          ],
          "type": "string"
        },
        "start_time": {
          "description": "Start timestamp in seconds (optional)",
          "type": "string"
        }
      },
      "required": [
        "chat_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "read_p2p_messages",
    "description": "[User UAT] Read P2P (direct message) chat history using user_access_token. Works for chats the bot cannot access. Returns newest messages first by default. Auto-expands merge_forward messages into their child messages by default — disable with expand_merge_forward=false. Requires OAuth setup.",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Chat ID (numeric from create_p2p_chat, or oc_xxx from list_user_chats). Both formats work.",
          "type": "string"
        },
        "end_time": {
          "description": "End timestamp in seconds (optional)",
          "type": "string"
        },
        "expand_merge_forward": {
          "description": "Auto-expand merge_forward placeholders into their child messages (default true). Children carry parentMessageId; use that id (not the child id) with download_message_resource (kind=image or file).",
          "type": "boolean"
        },
        "page_size": {
          "description": "Messages to fetch (default 20, max 50)",
          "type": "number"
        },
        "sort_type": {
          "description": "Sort order (default: ByCreateTimeDesc = newest first)",
          "enum": [
            "ByCreateTimeDesc",
            "ByCreateTimeAsc"
          ],
          "type": "string"
        },
        "start_time": {
          "description": "Start timestamp in seconds (optional)",
          "type": "string"
        }
      },
      "required": [
        "chat_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "reply_message",
    "description": "[Official API] Reply to a specific message by message_id (as bot). Only works for text messages; other types return error 230054.",
    "inputSchema": {
      "properties": {
        "message_id": {
          "description": "Message ID to reply to (om_xxx)",
          "type": "string"
        },
        "text": {
          "description": "Reply text",
          "type": "string"
        }
      },
      "required": [
        "message_id",
        "text"
      ],
      "type": "object"
    }
  },
  {
    "name": "respond_calendar_event",
    "description": "[Official API + UAT, v1.3.7] Respond to an event invitation. The current identity must be in the event's attendee list. Requires `calendar:calendar.event:write` scope.",
    "inputSchema": {
      "properties": {
        "calendar_id": {
          "description": "Calendar ID",
          "type": "string"
        },
        "event_id": {
          "description": "Event ID",
          "type": "string"
        },
        "rsvp_status": {
          "description": "Your response",
          "enum": [
            "accept",
            "decline",
            "tentative"
          ],
          "type": "string"
        }
      },
      "required": [
        "calendar_id",
        "event_id",
        "rsvp_status"
      ],
      "type": "object"
    }
  },
  {
    "name": "search_contacts",
    "description": "[User Identity] Search Feishu users, bots, or group chats by name. Returns IDs.",
    "inputSchema": {
      "properties": {
        "query": {
          "description": "Search keyword",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "search_docs",
    "description": "[Official API] Search Feishu documents by keyword.",
    "inputSchema": {
      "properties": {
        "query": {
          "description": "Search keyword",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "search_wiki",
    "description": "[Official API] Search Wiki nodes by keyword.",
    "inputSchema": {
      "properties": {
        "query": {
          "description": "Search keyword",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "send_as_user",
    "description": "[User Identity] Send a text message as the logged-in Feishu user. Supports reply threading and real @-mentions (triggers push notifications).",
    "inputSchema": {
      "properties": {
        "ats": {
          "description": "Optional @-mentions. Each entry: {userId: \"ou_xxx\", name: \"DisplayName\"}. The text must contain each @<name> marker in order — it gets spliced into a real AT element so the mentioned user receives a notification.",
          "items": {
            "properties": {
              "marker": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "userId": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "type": "array"
        },
        "chat_id": {
          "description": "Target chat ID. Numeric (from create_p2p_chat / search) preferred; oc_xxx is auto-resolved via getChatInfo + cookie search since v1.3.7 (C1.4).",
          "type": "string"
        },
        "parent_id": {
          "description": "Parent message ID (for nested reply, optional)",
          "type": "string"
        },
        "root_id": {
          "description": "Thread root message ID (for reply, optional)",
          "type": "string"
        },
        "text": {
          "description": "Message text. If `ats` is provided, include the display marker for each @ in this text (default marker is `@<name>`).",
          "type": "string"
        }
      },
      "required": [
        "chat_id",
        "text"
      ],
      "type": "object"
    }
  },
  {
    "name": "send_card_as_user",
    "description": "[v1.3.6+: bot-routed default] Send an interactive card to a chat. **Identity defaults to BOT** because user-identity card sending requires reverse-engineering the Feishu web protobuf (deferred to v1.3.9; v1.3.8 shipped the capture/decode tooling). The tool name keeps the \"as_user\" suffix so callers don't have to migrate when v1.3.9 lands; once user-identity is implemented the default flips. Pass `card` as a JSON object (Feishu card schema). To force bot explicitly set via=\"bot\".",
    "inputSchema": {
      "properties": {
        "card": {
          "description": "Feishu card JSON. See https://open.feishu.cn/cardkit for the schema; build cards visually then paste the resulting JSON here."
        },
        "chat_id": {
          "description": "Target chat_id (oc_xxx) or open_id",
          "type": "string"
        },
        "via": {
          "description": "Identity to send as. Default \"bot\". \"user\" returns an explicit not-yet-implemented error in v1.3.6.",
          "enum": [
            "bot",
            "user"
          ],
          "type": "string"
        }
      },
      "required": [
        "card",
        "chat_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "send_file_as_user",
    "description": "[User Identity] Send a file as the logged-in user. Requires file_key (upload via Official API first).",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Target chat ID. Numeric preferred; oc_xxx is auto-resolved (v1.3.7 C1.4).",
          "type": "string"
        },
        "file_key": {
          "description": "File key from upload",
          "type": "string"
        },
        "file_name": {
          "description": "Display file name",
          "type": "string"
        },
        "root_id": {
          "description": "Thread root message ID (optional)",
          "type": "string"
        }
      },
      "required": [
        "chat_id",
        "file_key",
        "file_name"
      ],
      "type": "object"
    }
  },
  {
    "name": "send_image_as_user",
    "description": "[User Identity] Send an image as the logged-in user. Requires image_key (upload via Official API first).",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Target chat ID. Numeric preferred; oc_xxx is auto-resolved (v1.3.7 C1.4).",
          "type": "string"
        },
        "image_key": {
          "description": "Image key from upload (img_v2_xxx or img_v3_xxx)",
          "type": "string"
        },
        "root_id": {
          "description": "Thread root message ID (optional)",
          "type": "string"
        }
      },
      "required": [
        "chat_id",
        "image_key"
      ],
      "type": "object"
    }
  },
  {
    "name": "send_message_as_bot",
    "description": "[Official API] Send a message as the bot to any chat. Supports text, post, interactive, etc. This is the reliable path for @-mentions: include `<at user_id=\"ou_xxx\">Name</at>` inline in text content and Feishu resolves it to a real @-notification.",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Target chat_id (oc_xxx) or open_id",
          "type": "string"
        },
        "content": {
          "description": "Message content (string or object, auto-serialized). Plain text: {\"text\":\"hello\"}. Text with @-mention: {\"text\":\"<at user_id=\\\"ou_xxx\\\">Alice</at> hi\"} — the inline tag becomes a real @-notification."
        },
        "msg_type": {
          "description": "Message type: text, post, image, interactive, etc.",
          "enum": [
            "text",
            "post",
            "image",
            "interactive",
            "share_chat",
            "share_user",
            "audio",
            "media",
            "file",
            "sticker"
          ],
          "type": "string"
        }
      },
      "required": [
        "chat_id",
        "content",
        "msg_type"
      ],
      "type": "object"
    }
  },
  {
    "name": "send_post_as_user",
    "description": "[User Identity] Send a rich text (POST) message with title and formatted paragraphs. Supports real @-mentions that trigger notifications.",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Target chat ID. Numeric preferred; oc_xxx is auto-resolved (v1.3.7 C1.4).",
          "type": "string"
        },
        "paragraphs": {
          "description": "Array of paragraphs. Each paragraph is an array of elements:\n• {tag:\"text\",text:\"...\"} — plain text\n• {tag:\"a\",href:\"https://...\",text:\"display\"} — hyperlink\n• {tag:\"at\",userId:\"ou_xxx\",name:\"Display Name\"} — real @-mention (triggers notification)",
          "items": {
            "items": {
              "type": "object"
            },
            "type": "array"
          },
          "type": "array"
        },
        "root_id": {
          "description": "Thread root message ID (optional)",
          "type": "string"
        },
        "title": {
          "description": "Post title (optional)",
          "type": "string"
        }
      },
      "required": [
        "chat_id",
        "paragraphs"
      ],
      "type": "object"
    }
  },
  {
    "name": "send_to_group",
    "description": "[User Identity] Search group by name → send text message. All in one step.",
    "inputSchema": {
      "properties": {
        "ats": {
          "description": "Optional @-mentions that trigger real notifications. Each entry: {userId, name}. Text must contain `@<name>` marker for each entry.",
          "items": {
            "properties": {
              "marker": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "userId": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "type": "array"
        },
        "group_name": {
          "description": "Group chat name",
          "type": "string"
        },
        "text": {
          "description": "Message text",
          "type": "string"
        }
      },
      "required": [
        "group_name",
        "text"
      ],
      "type": "object"
    }
  },
  {
    "name": "send_to_user",
    "description": "[User Identity] Search user by name → create P2P chat → send text message. All in one step.",
    "inputSchema": {
      "properties": {
        "ats": {
          "description": "Optional @-mentions. Same format as send_as_user.ats: [{userId, name}]. Text must contain the `@<name>` marker for each entry.",
          "items": {
            "properties": {
              "marker": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "userId": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "type": "array"
        },
        "text": {
          "description": "Message text",
          "type": "string"
        },
        "user_name": {
          "description": "Recipient name (Chinese or English)",
          "type": "string"
        }
      },
      "required": [
        "text",
        "user_name"
      ],
      "type": "object"
    }
  },
  {
    "name": "switch_profile",
    "description": "[Plugin] Switch the active identity profile. Subsequent tool calls use the new profile's credentials. Cached client instances are reset so the next call rebuilds against the new creds.",
    "inputSchema": {
      "properties": {
        "name": {
          "description": "Profile name. \"default\" for top-level env vars; any key from LARK_PROFILES_JSON otherwise.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "name": "update_calendar_event",
    "description": "[Official API + UAT, v1.3.7] Patch fields on an existing calendar event. Pass only the fields you want to change. Requires `calendar:calendar.event:write` scope.",
    "inputSchema": {
      "properties": {
        "attendee_ability": {
          "enum": [
            "none",
            "can_see_others",
            "can_invite_others",
            "can_modify_event"
          ],
          "type": "string"
        },
        "calendar_id": {
          "description": "Calendar ID",
          "type": "string"
        },
        "description": {
          "description": "New description (optional)",
          "type": "string"
        },
        "end_time": {
          "description": "A time object: {timestamp:\"<unix-seconds>\", timezone?:\"Asia/Shanghai\"} OR {date:\"YYYY-MM-DD\"} for all-day events.",
          "type": "object"
        },
        "event_id": {
          "description": "Event ID",
          "type": "string"
        },
        "free_busy_status": {
          "enum": [
            "busy",
            "free"
          ],
          "type": "string"
        },
        "location": {
          "description": "New location object (optional)",
          "type": "object"
        },
        "need_notification": {
          "description": "Whether to notify attendees of the update",
          "type": "boolean"
        },
        "recurrence": {
          "description": "RRULE string",
          "type": "string"
        },
        "reminders": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "start_time": {
          "description": "A time object: {timestamp:\"<unix-seconds>\", timezone?:\"Asia/Shanghai\"} OR {date:\"YYYY-MM-DD\"} for all-day events.",
          "type": "object"
        },
        "summary": {
          "description": "New title (optional)",
          "type": "string"
        },
        "visibility": {
          "enum": [
            "default",
            "public",
            "private"
          ],
          "type": "string"
        }
      },
      "required": [
        "calendar_id",
        "event_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "update_group",
    "description": "[Official API] Update group chat name or description.",
    "inputSchema": {
      "properties": {
        "chat_id": {
          "description": "Chat ID (oc_xxx)",
          "type": "string"
        },
        "description": {
          "description": "New description (optional)",
          "type": "string"
        },
        "name": {
          "description": "New group name (optional)",
          "type": "string"
        }
      },
      "required": [
        "chat_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "update_message",
    "description": "[Official API] Edit a sent message (bot can only edit its own messages). Feishu supports edit only for `text` and `interactive` (card) messages — other types (post, image, file, etc.) are rejected by the API.",
    "inputSchema": {
      "properties": {
        "content": {
          "description": "New content. For text: {\"text\":\"updated text\"}. For interactive: full card JSON."
        },
        "message_id": {
          "description": "Message ID (om_xxx)",
          "type": "string"
        },
        "msg_type": {
          "description": "Message type: text or interactive. Other types are not editable per Feishu API.",
          "enum": [
            "text",
            "interactive"
          ],
          "type": "string"
        }
      },
      "required": [
        "content",
        "message_id",
        "msg_type"
      ],
      "type": "object"
    }
  },
  {
    "name": "update_task",
    "description": "[Official API + UAT, v1.3.7] Patch a task. **update_fields** is required by Feishu — list which fields to update (e.g. [\"summary\",\"due\",\"completed_at\"]).",
    "inputSchema": {
      "properties": {
        "task": {
          "description": "Field values. E.g. {summary:\"new title\", due:{timestamp:\"1717939200000\"}}.",
          "type": "object"
        },
        "task_guid": {
          "description": "Task GUID",
          "type": "string"
        },
        "update_fields": {
          "description": "Required. Names of fields to update. E.g. [\"summary\",\"description\",\"due\",\"completed_at\",\"start\",\"extra\",\"repeat_rule\"]. Feishu only patches fields listed here, ignoring other keys in `task`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "task",
        "task_guid",
        "update_fields"
      ],
      "type": "object"
    }
  },
  {
    "name": "update_wiki_node",
    "description": "[Official API] Rename a Wiki node (only `title` is updatable via the wiki API; the underlying resource content is edited via docx/bitable/sheet tools).",
    "inputSchema": {
      "properties": {
        "node_token": {
          "description": "Wiki node token (wikcnXXX)",
          "type": "string"
        },
        "space_id": {
          "description": "Wiki space ID",
          "type": "string"
        },
        "title": {
          "description": "New title",
          "type": "string"
        }
      },
      "required": [
        "node_token",
        "space_id",
        "title"
      ],
      "type": "object"
    }
  },
  {
    "name": "upload_bitable_attachment",
    "description": "[Official API] Upload a file as a Bitable attachment (drive/v1/medias/upload_all with parent_type=bitable_image or bitable_file). Returns file_token suitable for writing into a Bitable Attachment-type field via batch_create/update_bitable_records (the field value should be [{file_token}]).",
    "inputSchema": {
      "properties": {
        "app_token": {
          "description": "Bitable app token (the bascn... or basc... id)",
          "type": "string"
        },
        "file_path": {
          "description": "Absolute path to the file on disk",
          "type": "string"
        },
        "kind": {
          "description": "Whether the attachment is an image (bitable_image) or a generic file (bitable_file). Default: file.",
          "enum": [
            "image",
            "file"
          ],
          "type": "string"
        }
      },
      "required": [
        "app_token",
        "file_path"
      ],
      "type": "object"
    }
  },
  {
    "name": "upload_drive_file",
    "description": "[Official API] Upload a file from disk to a Feishu Drive folder (drive/v1/files/upload_all, parent_type=explorer). Returns file_token + url. If wiki_space_id is provided, the uploaded file is then attached to that Wiki space via move_docs_to_wiki (obj_type=file). UAT-first with app fallback.",
    "inputSchema": {
      "properties": {
        "file_path": {
          "description": "Absolute path to the file on disk",
          "type": "string"
        },
        "folder_token": {
          "description": "Destination folder token. Use list_files to find one, or pass the user \"我的空间\" root token.",
          "type": "string"
        },
        "wiki_parent_node_token": {
          "description": "Optional. Parent node under which to attach in the Wiki space.",
          "type": "string"
        },
        "wiki_space_id": {
          "description": "Optional. If set, also attach the uploaded file to this Wiki space.",
          "type": "string"
        }
      },
      "required": [
        "file_path",
        "folder_token"
      ],
      "type": "object"
    }
  },
  {
    "name": "upload_file",
    "description": "[Official API] Upload a file to Feishu. Returns file_key for use with send_file_as_user.",
    "inputSchema": {
      "properties": {
        "file_name": {
          "description": "Display file name (optional, defaults to basename)",
          "type": "string"
        },
        "file_path": {
          "description": "Absolute path to the file on disk",
          "type": "string"
        },
        "file_type": {
          "description": "File type (default: stream for generic files)",
          "enum": [
            "opus",
            "mp4",
            "pdf",
            "doc",
            "xls",
            "ppt",
            "stream"
          ],
          "type": "string"
        }
      },
      "required": [
        "file_path"
      ],
      "type": "object"
    }
  },
  {
    "name": "upload_image",
    "description": "[Official API] Upload an image file to Feishu. Returns image_key for use with send_image_as_user.",
    "inputSchema": {
      "properties": {
        "image_path": {
          "description": "Absolute path to the image file on disk",
          "type": "string"
        },
        "image_type": {
          "description": "Image usage type (default: message)",
          "enum": [
            "message",
            "avatar"
          ],
          "type": "string"
        }
      },
      "required": [
        "image_path"
      ],
      "type": "object"
    }
  }
]
